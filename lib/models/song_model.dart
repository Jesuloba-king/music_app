class Song {
  final String songName, artistName, albumArtImagePath, audioPath;

  Song(
      {required this.songName,
      required this.artistName,
      required this.albumArtImagePath,
      required this.audioPath});
}
